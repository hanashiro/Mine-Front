'use strict';

/**
 * @ngdoc function
 * @name mineApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the mineApp
 */
angular.module('mineApp')
  .controller('LoginCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
